# hermes-nexus 依赖清单（按 .venv 实际安装版本记录）
# 安装：uv pip install --python .venv/bin/python --index-url https://mirrors.aliyun.com/pypi/simple -r requirements.txt
#
# --- 运行时 ---
fastapi==0.141.1
pyyaml==6.0.3
requests==2.34.2

# --- CLI（cli.py）---
fire==0.7.1
prompt_toolkit==3.0.53

# --- 测试 ---
pytest==9.1.1
