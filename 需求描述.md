## 需求来源
我正在搭建一个人机交互工具，需要提供一个fastapi服务，用于作为人机交互的mock。

## 需求模块
接口模块：部署fastapi服务，提供接口服务
Prompt组装：依据请求的输入，组装system_prompt
Agent交互：在特定渠道与用户交互（文本），具体渠道需要进行设计
聊天记录结果回传：除system_prompt，返回其他消息

## 组件
main.py fastapi函数
src/build_prompt.py 定义prompt，并根据请求输入中的sections字段填充完善的system_prompt，输入参考 ./望京恒电团建_用餐需求.json
src/channel.py 实现一个可以mock和用户交流的渠道,可以是界面，也可以是终端，需要有收集完信息或对话终止后自动关闭的功能
src/chat.py main.py引用，流程是创建system_prompt，发起一个沟通线程，进行多轮对话（调用llm），判断沟通是否结束，结束后关闭沟通线程，然后组织消息记录，返回
test/ 测试用路径